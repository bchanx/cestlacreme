import React from 'react';

var Menu = React.createClass({
  render: function() {
    return (
      <div className="menu">
        This is the menu of brulee's
      </div>
    );
  }
});

export default Menu;
